#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	go build -o hello .

override_dh_auto_install:
	install -D -m 755 hello debian/hello/usr/bin/hello

override_dh_dwz:
	# Skip dwz for Go binaries
